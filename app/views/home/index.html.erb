<% provide(:title, 'All of our users') %>

<h2>All of our users</h2>

<% if @users.count != 0 %>
<table class = "table">
<tr>
	<% @users.each do |user| %>
	<td><%= link_to user.email, user_path(user) %></td>
	<% end %>
</tr>
</table>
<% else %>
<p>We have no users currently</p>
<% end %>





